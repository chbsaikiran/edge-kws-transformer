# Mac (training / conversion / verification) for the PyTorch -> ONNX pipeline
torch
torchaudio      # only used for MelSpectrogram/FrequencyMasking/TimeMasking transforms;
                # dataset file I/O deliberately avoids torchaudio.load (see kws_common_torch.py
                # docstring -- current torchaudio hard-requires TorchCodec + system FFmpeg to
                # decode anything, which we sidestep with a stdlib `wave` reader instead)
onnx
onnxruntime
onnxscript>=0.7 # required by torch.onnx.export's exporter; <0.7 pulls in an onnx_ir
                # release missing onnx_ir.schemas, which the exporter needs --
                # confirmed AttributeError with onnxscript 0.5.0 (onnx_ir 0.1.8)
numpy
tqdm

# Raspberry Pi (inference only) — use run_onnx_pi.py, NOT run_onnx_mac.py.
# It's a separate, self-contained script (own NumPy mel-spectrogram
# implementation, verified to match torchaudio's output to float32 precision)
# with zero torch/torchaudio/onnx dependency -- only:
#   pip install onnxruntime numpy
# (official onnxruntime wheels cover linux_aarch64, so this is a light install)
