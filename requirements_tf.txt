# Mac (training / conversion / verification)
tensorflow==2.18.1
numpy
tqdm

# Deliberately NOT installing tensorflow-metal: verified it produces
# numerically wrong gradients for this model (see train_tf.py's
# set_visible_devices([], "GPU") comment) -- training is CPU-only for now.

# Raspberry Pi (inference only) — install ONE of these instead of tensorflow:
#   pip install tflite-runtime         # if a wheel exists for your Pi's OS/Python
#   pip install ai-edge-litert         # newer replacement for tflite-runtime
# Then in run_tflite_mac.py, swap the "Mac" import block for the "Raspberry Pi" one.
